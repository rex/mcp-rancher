# This file is generated by scripts/codegen from
# catalog/curated_tools/statefulsets.yml. Do not edit by hand.
# Regenerate with: make codegen

"""Curated Rancher statefulset tools."""

from __future__ import annotations

from rancher_mcp.audit import audit_mutation
from rancher_mcp.clients.management import ManagementDiscoveryClient, RancherManagementClient
from rancher_mcp.config import AppSettings, get_settings
from rancher_mcp.exceptions import RancherCapabilityError
from rancher_mcp.models.resources import RancherCuratedDeleteResult
from rancher_mcp.models.workloads import RancherStatefulSetDetail, RancherStatefulSetList
from rancher_mcp.rate_limit import rate_limit_writes
from rancher_mcp.services.instances import resolve_instance
from rancher_mcp.services.resource_queries import build_steve_list_query_params
from rancher_mcp.services.resources.builders_pagination import next_page_token_from_payload
from rancher_mcp.services.safety import ensure_instance_writable
from rancher_mcp.tools.support.values import mapping_value, string_dict
from rancher_mcp.tools.workloads.paths import workload_collection_path, workload_resource_path
from rancher_mcp.tools.workloads.shared import (
    items,
    statefulset_summary_from_payload,
)


async def _fetch_statefulsets_list(
    instance_name: str,
    cluster_id: str,
    namespace: str,
    ready: bool | None,
    limit: int | None,
    label_selector: str | None,
    field_selector: str | None,
    client: ManagementDiscoveryClient,
    page_token: str | None = None,
) -> RancherStatefulSetList:
    """Fetch and normalize statefulsets through Rancher's raw Kubernetes proxy."""

    query_params = build_steve_list_query_params(
        limit=limit,
        continue_token=page_token,
        label_selector=label_selector,
        field_selector=field_selector,
    )
    payload = await client.get_json(
        workload_collection_path(cluster_id, namespace, "statefulsets"),
        params=query_params or None,
    )
    statefulsets = [statefulset_summary_from_payload(item) for item in items(payload)]
    if ready is not None:
        statefulsets = [statefulset for statefulset in statefulsets if statefulset.ready is ready]
    return RancherStatefulSetList(
        instance=instance_name,
        cluster_id=cluster_id,
        namespace=namespace,
        statefulset_count=len(statefulsets),
        next_page_token=next_page_token_from_payload(payload),
        applied_query_params=query_params,
        statefulsets=statefulsets,
        suggested_next_steps=[
            "rancher_statefulset_get",
            "rancher_pods_list",
        ],
    )


async def rancher_statefulsets_list(
    namespace: str,
    cluster_id: str = "local",
    ready: bool | None = None,
    limit: int | None = None,
    label_selector: str | None = None,
    field_selector: str | None = None,
    page_token: str | None = None,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherStatefulSetList:
    """List statefulsets in one namespace with typed summaries."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_statefulsets_list(
            instance_name,
            cluster_id,
            namespace,
            ready,
            limit,
            label_selector,
            field_selector,
            client,
            page_token,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_statefulsets_list(
            instance_name,
            cluster_id,
            namespace,
            ready,
            limit,
            label_selector,
            field_selector,
            managed_client,
            page_token,
        )


async def _fetch_statefulset_get(
    instance_name: str,
    cluster_id: str,
    namespace: str,
    statefulset_name: str,
    client: ManagementDiscoveryClient,
) -> RancherStatefulSetDetail:
    """Fetch and normalize one statefulset."""

    payload = await client.get_json(
        workload_resource_path(cluster_id, namespace, "statefulsets", statefulset_name)
    )
    summary = statefulset_summary_from_payload(payload)

    metadata = mapping_value(payload, "metadata") or {}
    annotations = mapping_value(metadata, "annotations") or {}
    detail = RancherStatefulSetDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "id": summary.id,
            "ready": summary.ready,
            "container_images": summary.container_images,
            "annotation_keys": sorted(string_dict(annotations)),
            "payload": dict(payload),
            "suggested_next_steps": ["rancher_statefulsets_list", "rancher_pods_list"],
        }
    )


async def rancher_statefulset_get(
    namespace: str,
    statefulset_name: str,
    cluster_id: str = "local",
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherStatefulSetDetail:
    """Fetch one statefulset by namespace and name."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_statefulset_get(
            instance_name, cluster_id, namespace, statefulset_name, client
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_statefulset_get(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            managed_client,
        )


async def _delete_statefulset(
    instance_name: str,
    cluster_id: str,
    namespace: str,
    statefulset_name: str,
    confirmation_phrase_used: str,
    client: ManagementDiscoveryClient,
) -> RancherCuratedDeleteResult:
    """Delete one statefulset; returns a typed delete result."""

    response_payload = await client.delete_json(
        workload_resource_path(cluster_id, namespace, "statefulsets", statefulset_name),
    )
    return RancherCuratedDeleteResult(
        instance=instance_name,
        plane="steve",
        resource_kind="statefulset",
        resource_name=statefulset_name,
        namespace=namespace,
        cluster_id=cluster_id,
        deleted=True,
        confirmation_phrase_used=confirmation_phrase_used,
        response_payload=dict(response_payload),
        suggested_next_steps=["rancher_statefulsets_list"],
    )


@audit_mutation(operation="statefulset_delete", plane="steve")
@rate_limit_writes
async def rancher_statefulset_delete(
    namespace: str,
    statefulset_name: str,
    confirmation: str,
    cluster_id: str = "local",
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherCuratedDeleteResult:
    """Delete one statefulset after the agent echoes the required confirmation phrase."""

    expected_phrase = f"delete statefulset {statefulset_name} in namespace {namespace}"
    if confirmation != expected_phrase:
        raise RancherCapabilityError(
            f"Delete confirmation did not match the required phrase: {expected_phrase!r}"
        )
    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    ensure_instance_writable(instance_name, instance_config)
    if client is not None:
        return await _delete_statefulset(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            expected_phrase,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _delete_statefulset(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            expected_phrase,
            managed_client,
        )


async def _patch_statefulset_scale(
    instance_name: str,
    cluster_id: str,
    namespace: str,
    statefulset_name: str,
    replicas: int,
    client: ManagementDiscoveryClient,
) -> RancherStatefulSetDetail:
    """Scale one statefulset via JSON merge-patch; returns the curated detail."""

    patch_subtree: dict[str, object] = {}
    patch_subtree["replicas"] = replicas
    if not patch_subtree:
        raise RancherCapabilityError(
            "No patch fields provided; every arg was None. Pass at least one field to update."
        )
    request_payload: dict[str, object] = {"spec": patch_subtree}
    payload = await client.patch_json(
        workload_resource_path(cluster_id, namespace, "statefulsets", statefulset_name),
        payload=request_payload,
    )
    summary = statefulset_summary_from_payload(payload)

    metadata = mapping_value(payload, "metadata") or {}
    annotations = mapping_value(metadata, "annotations") or {}
    detail = RancherStatefulSetDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "id": summary.id,
            "ready": summary.ready,
            "container_images": summary.container_images,
            "annotation_keys": sorted(string_dict(annotations)),
            "payload": dict(payload),
            "suggested_next_steps": ["rancher_statefulset_get", "rancher_pods_list"],
        }
    )


@audit_mutation(operation="statefulset_scale", plane="steve")
@rate_limit_writes
async def rancher_statefulset_scale(
    namespace: str,
    statefulset_name: str,
    replicas: int,
    cluster_id: str = "local",
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherStatefulSetDetail:
    """Scale one statefulset via JSON merge-patch."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    ensure_instance_writable(instance_name, instance_config)
    if client is not None:
        return await _patch_statefulset_scale(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            replicas,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _patch_statefulset_scale(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            replicas,
            managed_client,
        )


async def _patch_statefulset_set_labels(
    instance_name: str,
    cluster_id: str,
    namespace: str,
    statefulset_name: str,
    labels: dict[str, str],
    client: ManagementDiscoveryClient,
) -> RancherStatefulSetDetail:
    """Set_labels one statefulset via JSON merge-patch; returns the curated detail."""

    patch_subtree: dict[str, object] = {}
    patch_subtree["labels"] = labels
    if not patch_subtree:
        raise RancherCapabilityError(
            "No patch fields provided; every arg was None. Pass at least one field to update."
        )
    request_payload: dict[str, object] = {"metadata": patch_subtree}
    payload = await client.patch_json(
        workload_resource_path(cluster_id, namespace, "statefulsets", statefulset_name),
        payload=request_payload,
    )
    summary = statefulset_summary_from_payload(payload)

    metadata = mapping_value(payload, "metadata") or {}
    annotations = mapping_value(metadata, "annotations") or {}
    detail = RancherStatefulSetDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "id": summary.id,
            "ready": summary.ready,
            "container_images": summary.container_images,
            "annotation_keys": sorted(string_dict(annotations)),
            "payload": dict(payload),
            "suggested_next_steps": ["rancher_statefulset_get", "rancher_pods_list"],
        }
    )


@audit_mutation(operation="statefulset_set_labels", plane="steve")
@rate_limit_writes
async def rancher_statefulset_set_labels(
    namespace: str,
    statefulset_name: str,
    labels: dict[str, str],
    cluster_id: str = "local",
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherStatefulSetDetail:
    """Set_labels one statefulset via JSON merge-patch."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    ensure_instance_writable(instance_name, instance_config)
    if client is not None:
        return await _patch_statefulset_set_labels(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            labels,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _patch_statefulset_set_labels(
            instance_name,
            cluster_id,
            namespace,
            statefulset_name,
            labels,
            managed_client,
        )


async def rancher_statefulsets_list_tool(
    namespace: str,
    cluster_id: str = "local",
    ready: bool | None = None,
    limit: int | None = None,
    label_selector: str | None = None,
    field_selector: str | None = None,
    page_token: str | None = None,
    instance: str | None = None,
) -> RancherStatefulSetList:
    """Public MCP wrapper for curated statefulset list."""

    return await rancher_statefulsets_list(
        namespace=namespace,
        cluster_id=cluster_id,
        ready=ready,
        limit=limit,
        label_selector=label_selector,
        field_selector=field_selector,
        page_token=page_token,
        instance=instance,
    )


async def rancher_statefulset_get_tool(
    namespace: str,
    statefulset_name: str,
    cluster_id: str = "local",
    instance: str | None = None,
) -> RancherStatefulSetDetail:
    """Public MCP wrapper for curated statefulset detail."""

    return await rancher_statefulset_get(
        namespace=namespace,
        statefulset_name=statefulset_name,
        cluster_id=cluster_id,
        instance=instance,
    )


async def rancher_statefulset_delete_tool(
    namespace: str,
    statefulset_name: str,
    confirmation: str,
    cluster_id: str = "local",
    instance: str | None = None,
) -> RancherCuratedDeleteResult:
    """Public MCP wrapper for curated statefulset delete."""

    return await rancher_statefulset_delete(
        namespace=namespace,
        statefulset_name=statefulset_name,
        confirmation=confirmation,
        cluster_id=cluster_id,
        instance=instance,
    )


async def rancher_statefulset_scale_tool(
    namespace: str,
    statefulset_name: str,
    replicas: int,
    cluster_id: str = "local",
    instance: str | None = None,
) -> RancherStatefulSetDetail:
    """Public MCP wrapper for curated statefulset scale."""

    return await rancher_statefulset_scale(
        namespace=namespace,
        statefulset_name=statefulset_name,
        replicas=replicas,
        cluster_id=cluster_id,
        instance=instance,
    )


async def rancher_statefulset_set_labels_tool(
    namespace: str,
    statefulset_name: str,
    labels: dict[str, str],
    cluster_id: str = "local",
    instance: str | None = None,
) -> RancherStatefulSetDetail:
    """Public MCP wrapper for curated statefulset set_labels."""

    return await rancher_statefulset_set_labels(
        namespace=namespace,
        statefulset_name=statefulset_name,
        labels=labels,
        cluster_id=cluster_id,
        instance=instance,
    )

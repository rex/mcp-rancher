# This file is generated by scripts/codegen from
# catalog/curated_tools/namespaced_certificates.yml. Do not edit by hand.
# Regenerate with: make codegen

"""Curated Rancher namespaced_certificate tools."""

from __future__ import annotations

from rancher_mcp.clients.management import ManagementDiscoveryClient, RancherManagementClient
from rancher_mcp.config import AppSettings, get_settings
from rancher_mcp.models.certificates import (
    RancherNamespacedCertificateDetail,
    RancherNamespacedCertificateList,
)
from rancher_mcp.services.instances import resolve_instance
from rancher_mcp.tools.certificates.shared import (
    build_namespaced_certificate_query_params,
    data_items,
    namespaced_certificate_summary_from_payload,
)
from rancher_mcp.tools.support.values import mapping_value, string_dict


async def _fetch_namespaced_certificates_list(
    instance_name: str,
    limit: int | None,
    name: str | None,
    state: str | None,
    namespace_id: str | None,
    project_id: str | None,
    sort_by: str | None,
    reverse: bool | None,
    client: ManagementDiscoveryClient,
) -> RancherNamespacedCertificateList:
    """Fetch and normalize the Rancher namespaced_certificates collection."""

    query_params = build_namespaced_certificate_query_params(
        limit=limit,
        name=name,
        state=state,
        namespace_id=namespace_id,
        project_id=project_id,
        sort_by=sort_by,
        reverse=reverse,
    )
    payload = await client.get_json("/v3/namespacedcertificates", params=query_params or None)
    namespaced_certificates = [
        namespaced_certificate_summary_from_payload(item) for item in data_items(payload)
    ]
    return RancherNamespacedCertificateList(
        instance=instance_name,
        namespaced_certificate_count=len(namespaced_certificates),
        applied_query_params=query_params,
        namespaced_certificates=namespaced_certificates,
        suggested_next_steps=[
            "rancher_namespaced_certificate_get",
            "rancher_certificates_list",
        ],
    )


async def rancher_namespaced_certificates_list(
    limit: int | None = None,
    name: str | None = None,
    state: str | None = None,
    namespace_id: str | None = None,
    project_id: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherNamespacedCertificateList:
    """List namespaced_certificates with typed summaries."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_namespaced_certificates_list(
            instance_name,
            limit,
            name,
            state,
            namespace_id,
            project_id,
            sort_by,
            reverse,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_namespaced_certificates_list(
            instance_name,
            limit,
            name,
            state,
            namespace_id,
            project_id,
            sort_by,
            reverse,
            managed_client,
        )


async def _fetch_namespaced_certificate_get(
    instance_name: str,
    certificate_id: str,
    client: ManagementDiscoveryClient,
) -> RancherNamespacedCertificateDetail:
    """Fetch and normalize one namespaced_certificate."""

    payload = await client.get_json(f"/v3/namespacedcertificates/{certificate_id}")

    metadata = mapping_value(payload, "metadata") or {}
    annotations = mapping_value(metadata, "annotations") or {}
    detail = RancherNamespacedCertificateDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "annotation_keys": sorted(string_dict(annotations)),
            "action_keys": sorted(mapping_value(payload, "actions") or {}),
            "link_keys": sorted(mapping_value(payload, "links") or {}),
            "suggested_next_steps": [
                "rancher_namespaced_certificates_list",
                "rancher_norman_resource_get",
            ],
        }
    )


async def rancher_namespaced_certificate_get(
    certificate_id: str,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherNamespacedCertificateDetail:
    """Fetch one namespaced_certificate by name."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_namespaced_certificate_get(instance_name, certificate_id, client)
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_namespaced_certificate_get(
            instance_name,
            certificate_id,
            managed_client,
        )


async def rancher_namespaced_certificates_list_tool(
    limit: int | None = None,
    name: str | None = None,
    state: str | None = None,
    namespace_id: str | None = None,
    project_id: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
) -> RancherNamespacedCertificateList:
    """List namespaced_certificates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool."""

    return await rancher_namespaced_certificates_list(
        limit=limit,
        name=name,
        state=state,
        namespace_id=namespace_id,
        project_id=project_id,
        sort_by=sort_by,
        reverse=reverse,
        instance=instance,
    )


async def rancher_namespaced_certificate_get_tool(
    certificate_id: str,
    instance: str | None = None,
) -> RancherNamespacedCertificateDetail:
    """Fetch one namespaced_certificate and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect."""

    return await rancher_namespaced_certificate_get(
        certificate_id=certificate_id,
        instance=instance,
    )

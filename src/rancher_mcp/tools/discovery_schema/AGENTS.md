# Discovery Schema Tool Package

- Keep API-plane, Norman schema, and Steve schema logic in separate files.
- Shared payload normalization stays private to the package.
- Facades should preserve stable imports and registration points.

# This file is generated by scripts/codegen from
# catalog/curated_tools/global_role_bindings.yml. Do not edit by hand.
# Regenerate with: make codegen

"""Curated Rancher global_role_binding tools."""

from __future__ import annotations

from rancher_mcp.clients.management import ManagementDiscoveryClient, RancherManagementClient
from rancher_mcp.config import AppSettings, get_settings
from rancher_mcp.models.rbac import RancherGlobalRoleBindingDetail, RancherGlobalRoleBindingList
from rancher_mcp.services.instances import resolve_instance
from rancher_mcp.tools.rbac.shared import (
    binding_subject,
    build_global_role_binding_query_params,
    data_items,
    global_role_binding_summary_from_payload,
    link_keys,
)


async def _fetch_global_role_bindings_list(
    instance_name: str,
    limit: int | None,
    global_role_id: str | None,
    user_id: str | None,
    group_principal_id: str | None,
    name: str | None,
    sort_by: str | None,
    reverse: bool | None,
    client: ManagementDiscoveryClient,
) -> RancherGlobalRoleBindingList:
    """Fetch and normalize the Rancher global_role_bindings collection."""

    query_params = build_global_role_binding_query_params(
        limit=limit,
        global_role_id=global_role_id,
        user_id=user_id,
        group_principal_id=group_principal_id,
        name=name,
        sort_by=sort_by,
        reverse=reverse,
    )
    payload = await client.get_json("/v3/globalrolebindings", params=query_params or None)
    global_role_bindings = [
        global_role_binding_summary_from_payload(item) for item in data_items(payload)
    ]
    return RancherGlobalRoleBindingList(
        instance=instance_name,
        global_role_binding_count=len(global_role_bindings),
        applied_query_params=query_params,
        global_role_bindings=global_role_bindings,
        suggested_next_steps=[
            "rancher_global_role_binding_get",
            "rancher_global_roles_list",
        ],
    )


async def rancher_global_role_bindings_list(
    limit: int | None = None,
    global_role_id: str | None = None,
    user_id: str | None = None,
    group_principal_id: str | None = None,
    name: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherGlobalRoleBindingList:
    """List global_role_bindings with typed summaries."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_global_role_bindings_list(
            instance_name,
            limit,
            global_role_id,
            user_id,
            group_principal_id,
            name,
            sort_by,
            reverse,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_global_role_bindings_list(
            instance_name,
            limit,
            global_role_id,
            user_id,
            group_principal_id,
            name,
            sort_by,
            reverse,
            managed_client,
        )


async def _fetch_global_role_binding_get(
    instance_name: str,
    global_role_binding_id: str,
    client: ManagementDiscoveryClient,
) -> RancherGlobalRoleBindingDetail:
    """Fetch and normalize one global_role_binding."""

    payload = await client.get_json(f"/v3/globalrolebindings/{global_role_binding_id}")

    subject = binding_subject(payload)
    detail = RancherGlobalRoleBindingDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "subject_kind": subject[0],
            "subject_id": subject[1],
            "link_keys": link_keys(payload),
            "payload": dict(payload),
            "suggested_next_steps": [
                "rancher_global_role_bindings_list",
                "rancher_global_roles_list",
            ],
        }
    )


async def rancher_global_role_binding_get(
    global_role_binding_id: str,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherGlobalRoleBindingDetail:
    """Fetch one global_role_binding by name."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_global_role_binding_get(instance_name, global_role_binding_id, client)
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_global_role_binding_get(
            instance_name,
            global_role_binding_id,
            managed_client,
        )


async def rancher_global_role_bindings_list_tool(
    limit: int | None = None,
    global_role_id: str | None = None,
    user_id: str | None = None,
    group_principal_id: str | None = None,
    name: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
) -> RancherGlobalRoleBindingList:
    """List global_role_bindings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool."""

    return await rancher_global_role_bindings_list(
        limit=limit,
        global_role_id=global_role_id,
        user_id=user_id,
        group_principal_id=group_principal_id,
        name=name,
        sort_by=sort_by,
        reverse=reverse,
        instance=instance,
    )


async def rancher_global_role_binding_get_tool(
    global_role_binding_id: str,
    instance: str | None = None,
) -> RancherGlobalRoleBindingDetail:
    """Fetch one global_role_binding and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect."""

    return await rancher_global_role_binding_get(
        global_role_binding_id=global_role_binding_id,
        instance=instance,
    )

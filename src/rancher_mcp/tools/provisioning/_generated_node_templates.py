# This file is generated by scripts/codegen from
# catalog/curated_tools/node_templates.yml. Do not edit by hand.
# Regenerate with: make codegen

"""Curated Rancher node_template tools."""

from __future__ import annotations

from rancher_mcp.clients.management import ManagementDiscoveryClient, RancherManagementClient
from rancher_mcp.config import AppSettings, get_settings
from rancher_mcp.models.provisioning import RancherNodeTemplateDetail, RancherNodeTemplateList
from rancher_mcp.services.instances import resolve_instance
from rancher_mcp.tools.provisioning.shared import (
    build_node_template_query_params,
    data_items,
    node_template_summary_from_payload,
)
from rancher_mcp.tools.support.values import mapping_value, string_dict


async def _fetch_node_templates_list(
    instance_name: str,
    limit: int | None,
    driver: str | None,
    cloud_credential_id: str | None,
    name: str | None,
    state: str | None,
    sort_by: str | None,
    reverse: bool | None,
    client: ManagementDiscoveryClient,
) -> RancherNodeTemplateList:
    """Fetch and normalize the Rancher node_templates collection."""

    query_params = build_node_template_query_params(
        limit=limit,
        driver=driver,
        cloud_credential_id=cloud_credential_id,
        name=name,
        state=state,
        sort_by=sort_by,
        reverse=reverse,
    )
    payload = await client.get_json("/v3/nodetemplates", params=query_params or None)
    node_templates = [node_template_summary_from_payload(item) for item in data_items(payload)]
    return RancherNodeTemplateList(
        instance=instance_name,
        node_template_count=len(node_templates),
        applied_query_params=query_params,
        node_templates=node_templates,
        suggested_next_steps=[
            "rancher_node_template_get",
            "rancher_cloud_credentials_list",
        ],
    )


async def rancher_node_templates_list(
    limit: int | None = None,
    driver: str | None = None,
    cloud_credential_id: str | None = None,
    name: str | None = None,
    state: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherNodeTemplateList:
    """List node_templates with typed summaries."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_node_templates_list(
            instance_name,
            limit,
            driver,
            cloud_credential_id,
            name,
            state,
            sort_by,
            reverse,
            client,
        )
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_node_templates_list(
            instance_name,
            limit,
            driver,
            cloud_credential_id,
            name,
            state,
            sort_by,
            reverse,
            managed_client,
        )


async def _fetch_node_template_get(
    instance_name: str,
    template_id: str,
    client: ManagementDiscoveryClient,
) -> RancherNodeTemplateDetail:
    """Fetch and normalize one node_template."""

    payload = await client.get_json(f"/v3/nodetemplates/{template_id}")

    metadata = mapping_value(payload, "metadata") or {}
    annotations = mapping_value(metadata, "annotations") or {}
    detail = RancherNodeTemplateDetail.model_validate(payload)
    return detail.model_copy(
        update={
            "annotation_keys": sorted(string_dict(annotations)),
            "action_keys": sorted(mapping_value(payload, "actions") or {}),
            "link_keys": sorted(mapping_value(payload, "links") or {}),
            "payload": dict(payload),
            "suggested_next_steps": [
                "rancher_node_templates_list",
                "rancher_cloud_credentials_list",
            ],
        }
    )


async def rancher_node_template_get(
    template_id: str,
    instance: str | None = None,
    settings: AppSettings | None = None,
    client: ManagementDiscoveryClient | None = None,
) -> RancherNodeTemplateDetail:
    """Fetch one node_template by name."""

    resolved_settings = settings or get_settings()
    instance_name, instance_config = resolve_instance(resolved_settings, instance)
    if client is not None:
        return await _fetch_node_template_get(instance_name, template_id, client)
    async with RancherManagementClient(instance_name, instance_config) as managed_client:
        return await _fetch_node_template_get(
            instance_name,
            template_id,
            managed_client,
        )


async def rancher_node_templates_list_tool(
    limit: int | None = None,
    driver: str | None = None,
    cloud_credential_id: str | None = None,
    name: str | None = None,
    state: str | None = None,
    sort_by: str | None = None,
    reverse: bool | None = None,
    instance: str | None = None,
) -> RancherNodeTemplateList:
    """Public MCP wrapper for curated node_template list."""

    return await rancher_node_templates_list(
        limit=limit,
        driver=driver,
        cloud_credential_id=cloud_credential_id,
        name=name,
        state=state,
        sort_by=sort_by,
        reverse=reverse,
        instance=instance,
    )


async def rancher_node_template_get_tool(
    template_id: str,
    instance: str | None = None,
) -> RancherNodeTemplateDetail:
    """Public MCP wrapper for curated node_template detail."""

    return await rancher_node_template_get(
        template_id=template_id,
        instance=instance,
    )

# Ops Tool Standards

- Keep these tools read-only and focused on high-value operational summaries or failure finders.
- Reuse shared typed helpers before adding ad hoc payload parsing.
- Every new ops tool needs direct unit coverage for both the happy path and at least one filtering edge case.

# Resource Service Standards

- Keep schema, path, capability, and builder logic reusable across generic tool families.
- Prefer schema-driven capability checks over hand-maintained allowlists.
- Normalize write results into structured models instead of leaking raw HTTP behavior into tools.

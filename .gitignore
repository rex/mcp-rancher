
__pycache__/
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v*
.claude/last-pre-compact-snapshot.md
.claude/logs/
.claude/serena-initialized
.claude/settings.local.json
.context7-cache/
.coverage
.DS_Store
.env
.lab/
.mypy_cache/
.pyright/
.pytest_cache/
.ruff_cache/
.serena/
.tools/
.venv/
*.egg-info/
*.pyc
*.pyo
*.tfplan
# Agent caches and logs (agentic-skeleton PR1)
# MCP/tool caches
# Terraform agent artifacts
build/
coverage.xml
dist/
htmlcov/
images/
tfplan
tfplan.*

# Claude Code session transcripts
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# make capture-sweep output (devtools/capture_sweep/) — raw per-call JSON
# dumps + manifest from the live local lab. Never committed; also carried
# in .git/info/exclude, but that's per-clone only — this is the durable copy.
capture/
capture_manifest.json
agenteval/

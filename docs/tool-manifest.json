{
  "_generated": "GENERATED by scripts/generate_tool_manifest.py — do not edit. Regenerate: make tool-manifest",
  "tool_count": 321,
  "read_only_count": 178,
  "write_count": 143,
  "destructive_count": 38,
  "tools": [
    {
      "name": "rancher_api_plane_list",
      "description": "List the Norman management-API and Steve Kubernetes-proxy planes available\n    on an instance, each with its root path and link count, to orient before\n    choosing norman_* vs steve_* tools.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_auth_config_get",
      "description": "Fetch one auth_config and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "auth_config_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_auth_configs_list",
      "description": "List auth_configs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "access_mode",
          "required": false
        },
        {
          "name": "enabled",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "provider_type",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_backup_get",
      "description": "Fetch one backup and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_backup_set_annotations",
      "description": "Modify one backup in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_backup_set_labels",
      "description": "Modify one backup in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_backups_list",
      "description": "List backups as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_capability_domain_list",
      "description": "Report the capability catalog's resource domains — RBAC, storage, networking,\n    logging, and so on — with plane and resource counts per domain, useful for\n    orienting before drilling into one area.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "catalog",
          "required": false
        },
        {
          "name": "settings",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_catalog_get",
      "description": "Fetch one catalog and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "catalog_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_catalogs_list",
      "description": "List catalogs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "helm_version",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "kind",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_certificate_delete",
      "description": "Delete one cert_manager_certificate and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "certificate_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_certificate_get",
      "description": "Fetch one cert_manager_certificate and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "certificate_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_certificate_set_annotations",
      "description": "Modify one cert_manager_certificate in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "certificate_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_certificate_set_labels",
      "description": "Modify one cert_manager_certificate in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "certificate_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_certificates_list",
      "description": "List cert_manager_certificates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_kind",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_cluster_issuer_delete",
      "description": "Delete one cert_manager_cluster_issuer and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_issuer_name",
          "required": true
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_cluster_issuer_get",
      "description": "Fetch one cert_manager_cluster_issuer and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_issuer_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_cluster_issuer_set_annotations",
      "description": "Modify one cert_manager_cluster_issuer in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_issuer_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_cluster_issuer_set_labels",
      "description": "Modify one cert_manager_cluster_issuer in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_issuer_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_cluster_issuers_list",
      "description": "List cert_manager_cluster_issuers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_kind_used",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cert_manager_issuer_delete",
      "description": "Delete one cert_manager_issuer and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_issuer_get",
      "description": "Fetch one cert_manager_issuer and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_issuer_set_annotations",
      "description": "Modify one cert_manager_issuer in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_issuer_set_labels",
      "description": "Modify one cert_manager_issuer in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_name",
          "required": true
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cert_manager_issuers_list",
      "description": "List cert_manager_issuers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "issuer_kind_used",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_certificate_get",
      "description": "Fetch one certificate and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "certificate_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_certificates_list",
      "description": "List certificates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cis_scan_get",
      "description": "Fetch one cis_scan and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "scan_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cis_scan_profile_get",
      "description": "Fetch one cis_scan_profile and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "profile_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cis_scan_profiles_list",
      "description": "List cis_scan_profiles as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cis_scans_list",
      "description": "List cis_scans as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cloud_credential_get",
      "description": "Fetch one cloud_credential and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "credential_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cloud_credentials_list",
      "description": "List cloud_credentials as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "driver",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_alert_rule_get",
      "description": "Fetch one cluster_alert_rule and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "rule_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_alert_rules_list",
      "description": "List cluster_alert_rules as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "severity",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_driver_get",
      "description": "Fetch one cluster_driver and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "driver_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_drivers_list",
      "description": "List cluster_drivers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "active",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_events_list",
      "description": "List Kubernetes events in a namespace. Filter by event_type (Warning/Normal) or reason.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "event_type",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "reason",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_flow_delete",
      "description": "Delete one cluster_flow and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_flow_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_flow_get",
      "description": "Fetch one cluster_flow and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_flow_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_flow_set_annotations",
      "description": "Modify one cluster_flow in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_flow_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_flow_set_labels",
      "description": "Modify one cluster_flow in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_flow_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_flows_list",
      "description": "List cluster_flows as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_get",
      "description": "Fetch one cluster and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_health_check",
      "description": "Diagnose one cluster in a single call: state, condition failures, unhealthy\n    components, and a node-readiness rollup, plus a derived list of concrete issues —\n    prefer this over separately fetching cluster and node details.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_logging_get",
      "description": "Fetch one cluster_logging and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_logging_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_loggings_list",
      "description": "List cluster_loggings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "enable_json_parsing",
          "required": false
        },
        {
          "name": "include_system_component",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "output_flush_interval",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_nodes_summary",
      "description": "Roll up one cluster's nodes into ready, not-ready, and unschedulable counts\n    and report the tallies — a lighter-weight alternative to a full node listing\n    when only the totals matter.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_output_delete",
      "description": "Delete one cluster_output and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_output_name",
          "required": true
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_output_get",
      "description": "Fetch one cluster_output and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_output_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_output_set_annotations",
      "description": "Modify one cluster_output in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_output_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_output_set_labels",
      "description": "Modify one cluster_output in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cluster_output_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_outputs_list",
      "description": "List cluster_outputs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "output_type",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_policy_report_delete",
      "description": "Delete one cluster_policy_report and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_policy_report_get",
      "description": "Fetch one cluster_policy_report and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_policy_report_set_annotations",
      "description": "Modify one cluster_policy_report in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_policy_report_set_labels",
      "description": "Modify one cluster_policy_report in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cluster_policy_reports_list",
      "description": "List cluster_policy_reports as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_registration_token_get",
      "description": "Fetch one cluster_registration_token and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_registration_token_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_registration_tokens_list",
      "description": "List cluster_registration_tokens as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "namespace_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_role_template_binding_get",
      "description": "Fetch one cluster_role_template_binding and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_role_template_binding_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cluster_role_template_bindings_list",
      "description": "List cluster_role_template_bindings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "group_id",
          "required": false
        },
        {
          "name": "group_principal_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "namespace_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "role_template_id",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "user_id",
          "required": false
        },
        {
          "name": "user_principal_id",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_clusters_health_summary",
      "description": "Summarize health across every cluster on an instance in one call — per-cluster\n    state, node readiness, top issues, and a fleet-wide severity breakdown — the\n    fastest way to triage an entire estate.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_clusters_list",
      "description": "List clusters as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_config_map_apply",
      "description": "Apply one config_map to a desired state as a declarative PUT and return its typed detail — creating the object when absent, replacing it when present. This writes to the cluster.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": false
        },
        {
          "name": "binary_data",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "data",
          "required": true
        },
        {
          "name": "immutable",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_map_create",
      "description": "Create one config_map from the supplied spec and return the typed detail of the new object, including its server-assigned identity and initial state. This writes to the cluster.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "annotations",
          "required": false
        },
        {
          "name": "binary_data",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "data",
          "required": true
        },
        {
          "name": "immutable",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_map_delete",
      "description": "Delete one config_map and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_map_get",
      "description": "Fetch one config_map and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_map_set_annotations",
      "description": "Modify one config_map in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_map_set_labels",
      "description": "Modify one config_map in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "config_map_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_config_maps_list",
      "description": "List config_maps as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_cron_job_delete",
      "description": "Delete one cron_job and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_job_get",
      "description": "Fetch one cron_job and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_job_resume",
      "description": "Modify one cron_job in place (resume) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_job_set_annotations",
      "description": "Modify one cron_job in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_job_set_labels",
      "description": "Modify one cron_job in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_job_suspend",
      "description": "Modify one cron_job in place (suspend) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "cron_job_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "suspend",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_cron_jobs_list",
      "description": "List cron_jobs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "suspend",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_daemonset_delete",
      "description": "Delete one daemonset and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "daemonset_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_daemonset_get",
      "description": "Fetch one daemonset and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "daemonset_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_daemonset_set_annotations",
      "description": "Modify one daemonset in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "daemonset_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_daemonset_set_labels",
      "description": "Modify one daemonset in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "daemonset_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_daemonsets_list",
      "description": "List daemonsets as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_deployment_delete",
      "description": "Delete one deployment and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_get",
      "description": "Fetch one deployment and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_pause",
      "description": "Modify one deployment in place (pause) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_restart",
      "description": "Modify one deployment in place (restart) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_resume",
      "description": "Modify one deployment in place (resume) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_scale",
      "description": "Modify one deployment in place (scale) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replicas",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_set_annotations",
      "description": "Modify one deployment in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployment_set_labels",
      "description": "Modify one deployment in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "deployment_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_deployments_list",
      "description": "List deployments as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_endpoint_slice_delete",
      "description": "Delete one endpoint_slice and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "endpoint_slice_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_endpoint_slice_get",
      "description": "Fetch one endpoint_slice and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "endpoint_slice_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_endpoint_slice_set_annotations",
      "description": "Modify one endpoint_slice in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "endpoint_slice_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_endpoint_slice_set_labels",
      "description": "Modify one endpoint_slice in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "endpoint_slice_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_endpoint_slices_list",
      "description": "List endpoint_slices as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "address_type",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "target_service",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_etcd_backup_get",
      "description": "Fetch one etcd_backup and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "etcd_backup_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_etcd_backups_list",
      "description": "List etcd_backups as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "filename",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "manual",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_feature_get",
      "description": "Fetch one feature and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "feature_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_features_list",
      "description": "List features as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "enabled",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_failing_pods",
      "description": "Scan for pods stuck in CrashLoopBackOff, ImagePullBackOff, Pending, Failed, or\n    Running-but-not-Ready, and report each with its reason, restart count, and\n    owning controller.\n\n    Omit `namespace` to sweep the entire cluster in one call instead of one\n    namespace at a time.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_pdbs_blocking",
      "description": "Scan pod disruption budgets that currently allow zero voluntary evictions and\n    report each with its healthy and desired counts plus the blocking reason —\n    explains why a drain or rollout is stuck waiting.\n\n    Omit `namespace` to sweep the entire cluster in one call.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_services_without_endpoints",
      "description": "Scan services that have a selector but no matching endpoint addresses — a\n    fast signal that traffic is arriving nowhere — reporting each service's type\n    and selector.\n\n    Omit `namespace` to sweep the entire cluster in one call.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_stalled_rollouts",
      "description": "Scan deployments and statefulsets for replica counts that have not converged,\n    reporting each stalled rollout's desired, ready, and updated counts plus the\n    stuck reason.\n\n    Omit `namespace` to sweep the entire cluster instead of one namespace at a time.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_unbound_pvcs",
      "description": "Scan persistent volume claims stuck outside the Bound phase and report each\n    with its storage class, requested size, and stall reason — the quickest way\n    to spot storage blocking a pod from starting.\n\n    Omit `namespace` to sweep the entire cluster at once.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_find_unready_nodes",
      "description": "Identify nodes that are not Ready or have been marked unschedulable, reporting\n    each with its roles, reason, and how long the condition has held. Omit\n    `cluster_id` to sweep every cluster on the instance at once.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_fleet_workspace_get",
      "description": "Fetch one fleet_workspace and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "fleet_workspace_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_fleet_workspaces_list",
      "description": "List fleet_workspaces as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_flow_delete",
      "description": "Delete one flow and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "flow_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_flow_get",
      "description": "Fetch one flow and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "flow_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_flow_set_annotations",
      "description": "Modify one flow in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "flow_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_flow_set_labels",
      "description": "Modify one flow in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "flow_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_flows_list",
      "description": "List flows as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_global_role_binding_get",
      "description": "Fetch one global_role_binding and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "global_role_binding_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_global_role_bindings_list",
      "description": "List global_role_bindings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "global_role_id",
          "required": false
        },
        {
          "name": "group_principal_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "user_id",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_global_role_get",
      "description": "Fetch one global_role and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "global_role_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_global_roles_list",
      "description": "List global_roles as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "builtin",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "new_user_default",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_group_get",
      "description": "Fetch one group and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "group_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_groups_list",
      "description": "List groups as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscaler_delete",
      "description": "Delete one horizontal_pod_autoscaler and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "hpa_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscaler_get",
      "description": "Fetch one horizontal_pod_autoscaler and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "hpa_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscaler_set_annotations",
      "description": "Modify one horizontal_pod_autoscaler in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "hpa_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscaler_set_labels",
      "description": "Modify one horizontal_pod_autoscaler in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "hpa_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscaler_set_min_max",
      "description": "Modify one horizontal_pod_autoscaler in place (set min max) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "hpa_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "maxReplicas",
          "required": true
        },
        {
          "name": "minReplicas",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_horizontal_pod_autoscalers_list",
      "description": "List horizontal_pod_autoscalers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "target_kind",
          "required": false
        },
        {
          "name": "target_name",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_ingress_delete",
      "description": "Delete one ingress and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "ingress_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_ingress_get",
      "description": "Fetch one ingress and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "ingress_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_ingress_set_annotations",
      "description": "Modify one ingress in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "ingress_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_ingress_set_labels",
      "description": "Modify one ingress in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "ingress_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_ingresses_list",
      "description": "List ingresses as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "class_name",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_instance_list",
      "description": "List every Rancher instance this deployment knows about, each with its base\n    URL and target version, so an agent can pick a valid `instance` argument before\n    calling any other tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "settings",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_job_delete",
      "description": "Delete one job and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "job_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_job_get",
      "description": "Fetch one job and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "job_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_job_set_annotations",
      "description": "Modify one job in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "job_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_job_set_labels",
      "description": "Modify one job in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "job_name",
          "required": true
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_jobs_list",
      "description": "List jobs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "complete",
          "required": false
        },
        {
          "name": "failed_terminal",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_limit_range_delete",
      "description": "Delete one limit_range and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit_range_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_limit_range_get",
      "description": "Fetch one limit_range and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit_range_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_limit_range_set_annotations",
      "description": "Modify one limit_range in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit_range_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_limit_range_set_labels",
      "description": "Modify one limit_range in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "limit_range_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_limit_ranges_list",
      "description": "List limit_ranges as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_longhorn_backup_get",
      "description": "Fetch one longhorn_backup and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_backup_set_annotations",
      "description": "Modify one longhorn_backup in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_backup_set_labels",
      "description": "Modify one longhorn_backup in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "backup_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_backups_list",
      "description": "List longhorn_backups as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "state",
          "required": false
        },
        {
          "name": "volume_name",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_longhorn_node_get",
      "description": "Fetch one longhorn_node and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "node_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_node_set_annotations",
      "description": "Modify one longhorn_node in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "node_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_node_set_labels",
      "description": "Modify one longhorn_node in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "node_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_nodes_list",
      "description": "List longhorn_nodes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        },
        {
          "name": "schedulable",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_longhorn_snapshot_get",
      "description": "Fetch one longhorn_snapshot and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "snapshot_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_snapshot_set_annotations",
      "description": "Modify one longhorn_snapshot in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "snapshot_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_snapshot_set_labels",
      "description": "Modify one longhorn_snapshot in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "snapshot_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_snapshots_list",
      "description": "List longhorn_snapshots as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "volume",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_longhorn_volume_get",
      "description": "Fetch one longhorn_volume and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_volume_set_annotations",
      "description": "Modify one longhorn_volume in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_volume_set_labels",
      "description": "Modify one longhorn_volume in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_longhorn_volumes_list",
      "description": "List longhorn_volumes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "robustness",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_monitoring_status",
      "description": "Check if Rancher monitoring is enabled on a cluster and summarize its state.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_namespace_get",
      "description": "Fetch one namespace and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_namespace_set_annotations",
      "description": "Modify one namespace in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_namespace_set_labels",
      "description": "Modify one namespace in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_namespace_workloads_summary",
      "description": "Aggregate one namespace's pod-phase tallies and deployment, daemonset, and\n    statefulset readiness into a single rollup, saving the four separate calls\n    otherwise needed to answer \"is this namespace healthy\".",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_namespaced_certificate_get",
      "description": "Fetch one namespaced_certificate and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "certificate_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_namespaced_certificates_list",
      "description": "List namespaced_certificates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "namespace_id",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_namespaces_list",
      "description": "List namespaces as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "phase",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_network_policies_list",
      "description": "List network_policies as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_network_policy_delete",
      "description": "Delete one network_policy and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "network_policy_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_network_policy_get",
      "description": "Fetch one network_policy and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "network_policy_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_network_policy_set_annotations",
      "description": "Modify one network_policy in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "network_policy_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_network_policy_set_labels",
      "description": "Modify one network_policy in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "network_policy_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_node_cordon",
      "description": "Mark a Rancher-managed node unschedulable so the scheduler stops placing new\n    pods there, without evicting anything already running, and return the action\n    result — reversible with `rancher_node_uncordon`.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "node_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_node_driver_get",
      "description": "Fetch one node_driver and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "driver_id",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_node_drivers_list",
      "description": "List node_drivers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "active",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_node_get",
      "description": "Fetch one node and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "node_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_node_template_get",
      "description": "Fetch one node_template and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "template_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_node_templates_list",
      "description": "List node_templates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cloud_credential_id",
          "required": false
        },
        {
          "name": "driver",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_node_uncordon",
      "description": "Restore scheduling on a previously cordoned Rancher-managed node so the\n    scheduler resumes placing new pods there, and return the action result.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "node_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_nodes_list",
      "description": "List nodes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "role",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        },
        {
          "name": "unschedulable",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_norman_resource_action_invoke",
      "description": "Invoke a named Norman resource action from that resource's own `actions`\n    links, with an optional JSON body, and return its response — the escape\n    hatch for lifecycle operations no curated tool wraps yet.",
      "read_only": false,
      "destructive": true,
      "idempotent": null,
      "params": [
        {
          "name": "action_name",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "payload_json",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_apply",
      "description": "Replace one Norman resource's full body via PUT under any schema id and\n    return the updated object — the untyped escape hatch for updates no curated\n    apply tool yet covers.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_create",
      "description": "Create a new Norman resource from a raw JSON payload under any schema id and\n    return the resulting object — the untyped escape hatch for resource types\n    with no curated create tool yet.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_delete",
      "description": "Delete one Norman resource by schema id and resource id after an explicit\n    confirmation string, and report the deletion result — the untyped,\n    destructive escape hatch for resource types with no curated delete tool.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_get",
      "description": "Fetch one Norman resource's full untyped payload by schema id and resource\n    id — the escape hatch for resource types with no curated `rancher_*_get`\n    tool yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_link_follow",
      "description": "Follow a named Norman resource link from that resource's own `links` map\n    and return the linked payload — the escape hatch for related data no\n    curated tool exposes yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "link_name",
          "required": true
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_resource_list",
      "description": "List any Norman resource collection by raw schema id with full untyped\n    payloads and server-side filtering — the escape hatch for resource types\n    with no curated `rancher_*_list` tool yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "filters_json",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "marker",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "schema_id",
          "required": true
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_norman_resource_patch",
      "description": "Merge a partial JSON payload into one existing Norman resource under any\n    schema id and return the patched object — the untyped escape hatch for a\n    scoped field update with no curated patch tool.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_schema_get",
      "description": "Fetch one Norman schema's field definitions, required attributes, and\n    supported collection actions by schema id, for constructing a valid create,\n    apply, or patch payload.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_norman_schema_list",
      "description": "List every Norman resource schema Rancher exposes on the management plane,\n    identifying which type ids are valid for `rancher_norman_resource_list` and\n    its siblings.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_notifier_get",
      "description": "Fetch one notifier and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "notifier_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_notifiers_list",
      "description": "List notifiers as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_output_delete",
      "description": "Delete one output and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "output_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_output_get",
      "description": "Fetch one output and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "output_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_output_set_annotations",
      "description": "Modify one output in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "output_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_output_set_labels",
      "description": "Modify one output in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "output_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_outputs_list",
      "description": "List outputs as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "output_type",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claim_delete",
      "description": "Delete one persistent_volume_claim and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "claim_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claim_get",
      "description": "Fetch one persistent_volume_claim and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "claim_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claim_set_annotations",
      "description": "Modify one persistent_volume_claim in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "claim_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claim_set_labels",
      "description": "Modify one persistent_volume_claim in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "claim_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claim_set_size",
      "description": "Modify one persistent_volume_claim in place (set size) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "claim_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "storage",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_claims_list",
      "description": "List persistent_volume_claims as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "phase",
          "required": false
        },
        {
          "name": "storage_class_name",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_get",
      "description": "Fetch one persistent_volume and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_set_annotations",
      "description": "Modify one persistent_volume in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volume_set_labels",
      "description": "Modify one persistent_volume in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "volume_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_persistent_volumes_list",
      "description": "List persistent_volumes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "phase",
          "required": false
        },
        {
          "name": "storage_class_name",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_pod_delete",
      "description": "Delete one pod and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_disruption_budget_delete",
      "description": "Delete one pod_disruption_budget and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "budget_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_disruption_budget_get",
      "description": "Fetch one pod_disruption_budget and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "budget_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_disruption_budget_set_annotations",
      "description": "Modify one pod_disruption_budget in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "budget_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_disruption_budget_set_labels",
      "description": "Modify one pod_disruption_budget in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "budget_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_disruption_budgets_list",
      "description": "List pod_disruption_budgets as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_pod_get",
      "description": "Fetch one pod and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_logs",
      "description": "Fetch a pod container's recent log tail — the missing `kubectl logs`\n    verb. Omit `container` for a single-container pod; a multi-container pod\n    without one returns a clean error listing the available container names\n    so you can retry. Set `previous=true` to read the last terminated\n    instance's logs (diagnosing a crash loop) instead of the current one.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "container",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_name",
          "required": true
        },
        {
          "name": "previous",
          "required": false
        },
        {
          "name": "tail_lines",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_pod_monitor_delete",
      "description": "Delete one pod_monitor and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_monitor_get",
      "description": "Fetch one pod_monitor and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_monitor_set_annotations",
      "description": "Modify one pod_monitor in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_monitor_set_labels",
      "description": "Modify one pod_monitor in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_monitors_list",
      "description": "List pod_monitors as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_pod_set_annotations",
      "description": "Modify one pod in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pod_set_labels",
      "description": "Modify one pod in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "pod_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_pods_list",
      "description": "List pods as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "phase",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_policy_report_delete",
      "description": "Delete one policy_report and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_policy_report_get",
      "description": "Fetch one policy_report and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_policy_report_set_annotations",
      "description": "Modify one policy_report in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_policy_report_set_labels",
      "description": "Modify one policy_report in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "report_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_policy_reports_list",
      "description": "List policy_reports as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_priority_class_delete",
      "description": "Delete one priority_class and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "priority_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_priority_class_get",
      "description": "Fetch one priority_class and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "priority_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_priority_class_set_annotations",
      "description": "Modify one priority_class in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "priority_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_priority_class_set_labels",
      "description": "Modify one priority_class in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "priority_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_priority_classes_list",
      "description": "List priority_classes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "global_default",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "preemption_policy",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_project_get",
      "description": "Fetch one project and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "project_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_project_health_summary",
      "description": "Aggregate one project's namespaces, pod counts, failing-pod tallies, and\n    unhealthy-workload counts into a single rollup, saving the per-namespace calls\n    otherwise needed to answer \"is this project healthy\".",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "project_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_project_logging_get",
      "description": "Fetch one project_logging and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "project_logging_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_project_loggings_list",
      "description": "List project_loggings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "enable_json_parsing",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "output_flush_interval",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_project_role_template_binding_get",
      "description": "Fetch one project_role_template_binding and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "project_role_template_binding_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_project_role_template_bindings_list",
      "description": "List project_role_template_bindings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "group_id",
          "required": false
        },
        {
          "name": "group_principal_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "namespace_id",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "role_template_id",
          "required": false
        },
        {
          "name": "service_account",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "user_id",
          "required": false
        },
        {
          "name": "user_principal_id",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_projects_list",
      "description": "List projects as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_prometheus_rule_delete",
      "description": "Delete one prometheus_rule and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "rule_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_prometheus_rule_get",
      "description": "Fetch one prometheus_rule and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "rule_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_prometheus_rule_set_annotations",
      "description": "Modify one prometheus_rule in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "rule_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_prometheus_rule_set_labels",
      "description": "Modify one prometheus_rule in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "rule_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_prometheus_rules_list",
      "description": "List prometheus_rules as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_replica_set_delete",
      "description": "Delete one replica_set and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replica_set_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_replica_set_get",
      "description": "Fetch one replica_set and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replica_set_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_replica_set_set_annotations",
      "description": "Modify one replica_set in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replica_set_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_replica_set_set_labels",
      "description": "Modify one replica_set in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replica_set_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_replica_sets_list",
      "description": "List replica_sets as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_resource_events",
      "description": "List recent Kubernetes events for one named resource (Pod, Deployment,\n    PersistentVolumeClaim, ...) — \"what just happened to this thing\",\n    generalized from `pod_get`'s inlined pod-scoped events to any `kind`.\n    Most recent first, capped to the last ~20.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "kind",
          "required": true
        },
        {
          "name": "name",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_resource_quota_delete",
      "description": "Delete one resource_quota and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "resource_quota_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_resource_quota_get",
      "description": "Fetch one resource_quota and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "resource_quota_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_resource_quota_set_annotations",
      "description": "Modify one resource_quota in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "resource_quota_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_resource_quota_set_labels",
      "description": "Modify one resource_quota in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "resource_quota_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_resource_quotas_list",
      "description": "List resource_quotas as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_restore_get",
      "description": "Fetch one restore and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "restore_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_restore_set_annotations",
      "description": "Modify one restore in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "restore_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_restore_set_labels",
      "description": "Modify one restore in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "restore_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_restores_list",
      "description": "List restores as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_role_template_get",
      "description": "Fetch one role_template and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "role_template_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_role_templates_list",
      "description": "List role_templates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "administrative",
          "required": false
        },
        {
          "name": "builtin",
          "required": false
        },
        {
          "name": "cluster_creator_default",
          "required": false
        },
        {
          "name": "context",
          "required": false
        },
        {
          "name": "external",
          "required": false
        },
        {
          "name": "hidden",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "locked",
          "required": false
        },
        {
          "name": "name",
          "required": false
        },
        {
          "name": "project_creator_default",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_runtime_class_delete",
      "description": "Delete one runtime_class and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "runtime_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_runtime_class_get",
      "description": "Fetch one runtime_class and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "runtime_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_runtime_class_set_annotations",
      "description": "Modify one runtime_class in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "runtime_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_runtime_class_set_labels",
      "description": "Modify one runtime_class in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "runtime_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_runtime_classes_list",
      "description": "List runtime_classes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "handler",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_secret_create",
      "description": "Create one secret from the supplied spec and return the typed detail of the new object, including its server-assigned identity and initial state. This writes to the cluster.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "annotations",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "data",
          "required": false
        },
        {
          "name": "immutable",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "secret_name",
          "required": true
        },
        {
          "name": "secret_type",
          "required": false
        },
        {
          "name": "string_data",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_secret_delete",
      "description": "Delete one secret and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "secret_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_secret_get",
      "description": "Fetch one secret and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect. Sensitive values are withheld by default (only names/counts appear); pass reveal=true to receive the decoded values — each reveal is audited.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "reveal",
          "required": false
        },
        {
          "name": "secret_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_secret_set_annotations",
      "description": "Modify one secret in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "secret_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_secret_set_labels",
      "description": "Modify one secret in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "secret_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_secrets_list",
      "description": "List secrets as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "secret_type",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_server_health",
      "description": "Check the target instance's `/healthz` endpoint and report whether the Rancher\n    management server is reachable and responding — a first liveness probe before\n    deeper diagnosis.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_server_profile_get",
      "description": "Return static deployment metadata for one Rancher instance: its configured URL,\n    primary target version, and compatibility floor, without making a network call.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "catalog",
          "required": false
        },
        {
          "name": "settings",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_server_version",
      "description": "Fetch the Rancher server's reported version alongside this MCP server's own\n    build version, so an agent can confirm which API surface and capability set it\n    is talking to.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_service_account_delete",
      "description": "Delete one service_account and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_account_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_account_get",
      "description": "Fetch one service_account and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_account_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_account_set_annotations",
      "description": "Modify one service_account in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_account_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_account_set_labels",
      "description": "Modify one service_account in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_account_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_accounts_list",
      "description": "List service_accounts as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_service_delete",
      "description": "Delete one service and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_get",
      "description": "Fetch one service and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_monitor_delete",
      "description": "Delete one service_monitor and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_monitor_get",
      "description": "Fetch one service_monitor and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_monitor_set_annotations",
      "description": "Modify one service_monitor in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_monitor_set_labels",
      "description": "Modify one service_monitor in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_monitor_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_monitors_list",
      "description": "List service_monitors as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_service_set_annotations",
      "description": "Modify one service in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_set_labels",
      "description": "Modify one service in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_service_set_type",
      "description": "Modify one service in place (set type) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "service_name",
          "required": true
        },
        {
          "name": "type",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_services_list",
      "description": "List services as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_setting_get",
      "description": "Fetch one setting and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "setting_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_settings_list",
      "description": "List settings as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "customized",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "source",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_statefulset_delete",
      "description": "Delete one statefulset and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "statefulset_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_statefulset_get",
      "description": "Fetch one statefulset and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "statefulset_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_statefulset_scale",
      "description": "Modify one statefulset in place (scale) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "replicas",
          "required": true
        },
        {
          "name": "statefulset_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_statefulset_set_annotations",
      "description": "Modify one statefulset in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "statefulset_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_statefulset_set_labels",
      "description": "Modify one statefulset in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "statefulset_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_statefulsets_list",
      "description": "List statefulsets as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": true
        },
        {
          "name": "page_token",
          "required": false
        },
        {
          "name": "ready",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_steve_resource_action_invoke",
      "description": "Invoke a named Steve Kubernetes-proxy resource action with an optional JSON\n    body and return its response — the escape hatch for lifecycle operations no\n    curated tool wraps yet.",
      "read_only": false,
      "destructive": true,
      "idempotent": null,
      "params": [
        {
          "name": "action_name",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "payload_json",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_apply",
      "description": "Apply one Steve Kubernetes-proxy resource via Kubernetes server-side apply\n    under any schema id and return the updated object — the untyped escape hatch\n    for updates with no curated apply tool yet.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_manager",
          "required": false
        },
        {
          "name": "force",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_create",
      "description": "Create a new Steve Kubernetes-proxy resource from a raw JSON payload under any\n    schema id and return the resulting object — the untyped escape hatch when no\n    curated create tool exists for that kind.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_delete",
      "description": "Delete one Steve Kubernetes-proxy resource by schema id and resource id after\n    an explicit confirmation string, and report the deletion result — the untyped,\n    destructive escape hatch for kinds with no curated delete tool.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_get",
      "description": "Fetch one Steve Kubernetes-proxy resource's full untyped payload by schema id,\n    cluster, and resource id — the escape hatch for kinds with no curated\n    `rancher_*_get` tool yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_link_follow",
      "description": "Follow a named Steve Kubernetes-proxy resource link and return the linked\n    payload — the escape hatch for related data no curated tool exposes yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "link_name",
          "required": true
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_list",
      "description": "List any Steve Kubernetes-proxy resource collection by raw schema id, cluster,\n    and optional namespace with label or field selectors — the escape hatch for\n    kinds with no curated `rancher_*_list` tool yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "continue_token",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_patch",
      "description": "Merge a JSON merge-patch into one existing Steve Kubernetes-proxy resource\n    under any schema id and return the patched object — the untyped escape hatch\n    for a scoped field update with no curated patch tool.",
      "read_only": false,
      "destructive": false,
      "idempotent": false,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "payload_json",
          "required": true
        },
        {
          "name": "resource_id",
          "required": true
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_resource_watch",
      "description": "Stream change events for a Steve Kubernetes-proxy resource collection up to a\n    bounded event count and return them as one batch — the escape hatch for\n    watching kinds with no curated tool yet.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "field_selector",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "label_selector",
          "required": false
        },
        {
          "name": "max_events",
          "required": false
        },
        {
          "name": "namespace",
          "required": false
        },
        {
          "name": "params_json",
          "required": false
        },
        {
          "name": "schema_id",
          "required": true
        },
        {
          "name": "timeout_seconds",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_steve_schema_get",
      "description": "Fetch one Steve schema's field definitions and supported verbs for a target\n    cluster, for constructing a valid create, apply, patch, or watch payload by\n    schema id.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "schema_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_steve_schema_list",
      "description": "List every Steve Kubernetes-proxy schema available for a target cluster,\n    identifying which type ids are valid for `rancher_steve_resource_list` and\n    its siblings.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_storage_class_delete",
      "description": "Delete one storage_class and return a typed receipt of what was removed. Destructive and irreversible — the caller must first echo the exact confirmation phrase the tool requires.",
      "read_only": false,
      "destructive": true,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "confirmation",
          "required": true
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "storage_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_storage_class_get",
      "description": "Fetch one storage_class and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "storage_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_storage_class_set_annotations",
      "description": "Modify one storage_class in place (set annotations) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "annotations",
          "required": true
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "storage_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_storage_class_set_labels",
      "description": "Modify one storage_class in place (set labels) via a JSON merge-patch and return a mutation receipt — the before and after of only the changed fields, not the whole object. A targeted write.",
      "read_only": false,
      "destructive": false,
      "idempotent": true,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "labels",
          "required": true
        },
        {
          "name": "storage_class_name",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_storage_classes_list",
      "description": "List storage_classes as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "default_only",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "page_token",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_template_get",
      "description": "Fetch one template and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "template_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_template_version_get",
      "description": "Fetch one template_version and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "template_version_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_template_versions_list",
      "description": "List template_versions as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "external_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        },
        {
          "name": "version",
          "required": false
        },
        {
          "name": "version_name",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_templates_list",
      "description": "List templates as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "catalog_id",
          "required": false
        },
        {
          "name": "category",
          "required": false
        },
        {
          "name": "cluster_id",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "project_id",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    },
    {
      "name": "rancher_user_get",
      "description": "Fetch one user and return its full typed detail: the conditions, diagnostics, and derived fields the list summary leaves out. Call this once a list has identified which one to inspect.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "user_id",
          "required": true
        }
      ]
    },
    {
      "name": "rancher_users_list",
      "description": "List users as lightweight typed summaries — identity, state, and a per-item health rollup rather than full specs — so an agent can enumerate what exists before opening any one in detail with the matching get tool.",
      "read_only": true,
      "destructive": false,
      "idempotent": null,
      "params": [
        {
          "name": "enabled",
          "required": false
        },
        {
          "name": "instance",
          "required": false
        },
        {
          "name": "limit",
          "required": false
        },
        {
          "name": "me",
          "required": false
        },
        {
          "name": "reverse",
          "required": false
        },
        {
          "name": "sort_by",
          "required": false
        },
        {
          "name": "state",
          "required": false
        }
      ]
    }
  ]
}

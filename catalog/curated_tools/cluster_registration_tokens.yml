schema_version: 1

id: cluster_registration_tokens
pack: fleet_registration
display_name_singular: cluster_registration_token
display_name_plural: cluster_registration_tokens

plane: norman
transport: norman
namespaced: false
cluster_id_required: false
pagination: false

list_path: /v3/clusterregistrationtokens
detail_path: /v3/clusterregistrationtokens/{cluster_registration_token_id}

list_response_model: rancher_mcp.models.fleet_registration.RancherClusterRegistrationTokenList
detail_response_model: rancher_mcp.models.fleet_registration.RancherClusterRegistrationTokenDetail

shared_imports:
  - action_keys
  - cluster_registration_token_summary_from_payload
  - data_items
  - link_keys
summary_function: cluster_registration_token_summary_from_payload
query_builder_function: build_cluster_registration_token_query_params
query_builder_in_shared: true

operations: [list, get]

list:
  query_params: [limit, cluster_id, name, state, namespace_id, sort_by, reverse]
  filters: []
  count_field: cluster_registration_token_count
  collection_field: cluster_registration_tokens
  next_steps:
    - rancher_cluster_registration_token_get
    - rancher_clusters_list

get:
  arg_name: cluster_registration_token_id
  summary_copy_fields: []
  locals: []
  extras:
    - field: action_keys
      expression: action_keys(payload)
    - field: link_keys
      expression: link_keys(payload)
  include_action_keys: false
  include_link_keys: false
  include_payload: true
  next_steps:
    - rancher_cluster_registration_tokens_list
    - rancher_clusters_list

tools:
  list:
    name: rancher_cluster_registration_tokens_list
    description: List Rancher cluster registration tokens via the Norman /v3 API. Filter by cluster id, name, state, or namespace id.
    annotation_set: READ_ONLY
  get:
    name: rancher_cluster_registration_token_get
    description: Fetch one Rancher cluster registration token by id via the Norman /v3 API. Returns the curated summary plus action/link keys and the full payload.
    annotation_set: READ_ONLY

schema_version: 1

id: auth_configs
pack: auth_identity
display_name_singular: auth_config
display_name_plural: auth_configs

plane: norman
transport: norman
namespaced: false
cluster_id_required: false
pagination: false

list_path: /v3/authconfigs
detail_path: /v3/authconfigs/{auth_config_id}

list_response_model: rancher_mcp.models.auth_identity.RancherAuthConfigList
detail_response_model: rancher_mcp.models.auth_identity.RancherAuthConfigDetail

shared_imports:
  - auth_config_summary_from_payload
  - data_items
summary_function: auth_config_summary_from_payload
query_builder_function: build_auth_config_query_params
query_builder_in_shared: true

operations: [list, get]

list:
  query_params: [limit, enabled, provider_type, access_mode, sort_by, reverse]
  filters: []
  count_field: auth_config_count
  collection_field: auth_configs
  next_steps:
    - rancher_auth_config_get
    - rancher_users_list
    - rancher_groups_list

get:
  arg_name: auth_config_id
  summary_copy_fields: []
  locals: []
  extras: []
  include_action_keys: true
  include_link_keys: true
  include_payload: true
  next_steps:
    - rancher_auth_configs_list
    - rancher_users_list

tools:
  list:
    name: rancher_auth_configs_list
    description: List Rancher auth configs via the Norman /v3 API. Filter by enabled, provider type, access mode, or sort by any field.
    annotation_set: READ_ONLY
  get:
    name: rancher_auth_config_get
    description: Fetch one Rancher auth config by id via the Norman /v3 API. Returns the curated summary plus action/link keys and the full payload.
    annotation_set: READ_ONLY

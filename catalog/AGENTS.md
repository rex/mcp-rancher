# Catalog Folder Standards

## Purpose

This folder holds machine-readable capability inventory data for the Rancher MCP server.

## Rules

- Treat catalog files as durable product scope, not scratchpads.
- Keep identifiers stable once published.
- Update catalog files together with code and docs when capability scope changes.
- Keep generated files and transient exports out of this folder.

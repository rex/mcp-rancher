# Script Standards

- Keep scripts as thin entrypoints over reusable repo-local helpers.
- Do not import runtime MCP handlers just to perform lab-only or test-only work.
- Favor explicit CLI arguments with sensible repo-local defaults.

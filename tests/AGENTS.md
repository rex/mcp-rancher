# Test Standards

- Mirror source-level concerns in test files.
- Prefer direct handler tests over protocol-layer tests.
- Use explicit fixtures and avoid hidden global state.
- Clear settings and catalog caches between tests.

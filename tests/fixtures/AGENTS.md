# Fixture Standards

- Commit only sanitized, deterministic fixture data.
- Never commit raw captures, secrets, bearer tokens, or machine-local hostnames.
- Keep fixture names aligned with the capture manifest so regeneration stays obvious.
